# 大学竞赛活动报名系统 - 运行指南

## 🚀 快速启动指南

### 📋 环境要求

#### 必需软件
- **JDK**: 1.8 或以上版本
- **MySQL**: 8.0 或以上版本  
- **Redis**: 6.0 或以上版本
- **Node.js**: 12.0 或以上版本
- **Maven**: 3.6 或以上版本

#### 可选工具
- **IDE**: IntelliJ IDEA 或 Eclipse
- **数据库工具**: Navicat 或 MySQL Workbench
- **Redis工具**: Redis Desktop Manager

## 🗄️ 第一步：数据库配置

### 1. 安装MySQL
确保MySQL 8.0已安装并运行在端口3306

### 2. 创建数据库
```sql
CREATE DATABASE db_competitionapply CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 3. 导入数据
```bash
# 方法1：使用命令行
mysql -u root -p db_competitionapply < db_competitionapply.sql

# 方法2：使用Navicat等工具
# 打开Navicat -> 连接数据库 -> 运行SQL文件 -> 选择db_competitionapply.sql
```

### 4. 验证数据导入
登录MySQL查看表是否创建成功：
```sql
USE db_competitionapply;
SHOW TABLES;
-- 应该看到15个表：t_user, t_competition, t_team等
```

## 🔴 第二步：Redis配置

### 1. 启动Redis服务

#### Windows系统
```bash
# 进入Redis目录
cd Redis
# 启动Redis服务器
redis-server.exe
```

#### Linux/Mac系统
```bash
# 启动Redis
redis-server
# 或者使用系统服务
sudo systemctl start redis
```

### 2. 验证Redis连接
```bash
# 测试Redis连接
redis-cli ping
# 应该返回：PONG
```

## ⚙️ 第三步：后端配置与启动

### 1. 检查配置文件
打开 `118/competition_apply_system/src/main/resources/application.properties`

确认以下配置正确：
```properties
# 数据库配置（根据实际情况修改）
spring.datasource.username=root
spring.datasource.password=root  # 改为您的MySQL密码

# Redis配置
spring.redis.host=127.0.0.1
spring.redis.port=6379
```

### 2. 启动后端服务

#### 方法1：使用IDE
1. 用IntelliJ IDEA打开 `118/competition_apply_system` 项目
2. 等待Maven依赖下载完成
3. 找到 `CompetitionApplySystemApplication.java`
4. 右键 -> Run 'CompetitionApplySystemApplication'

#### 方法2：使用命令行
```bash
# 进入后端项目目录
cd 118/competition_apply_system

# 使用Maven启动
mvn spring-boot:run

# 或者先编译再运行
mvn clean package
java -jar target/BookManagerApi-1.0.5.jar
```

### 3. 验证后端启动
- 看到控制台输出：`Started CompetitionApplySystemApplication`
- 访问：http://localhost:8999/competition
- 应该看到Whitelabel Error Page（正常，因为没有默认页面）

## 🎨 第四步：前端配置与启动

### 1. 安装Node.js依赖
```bash
# 进入前端项目目录
cd 118/competitionapply-web

# 安装依赖（首次运行需要）
npm install

# 如果npm安装慢，可以使用cnpm或yarn
# npm install -g cnpm --registry=https://registry.npm.taobao.org
# cnpm install
```

### 2. 启动前端服务
```bash
# 开发模式启动
npm run serve
```

### 3. 验证前端启动
- 看到控制台输出：`App running at: http://localhost:8080/`
- 浏览器自动打开或手动访问：http://localhost:8080
- 应该看到登录页面

## 🔐 第五步：系统登录测试

### 默认账号信息

#### 管理员账号
- **用户名**: admin
- **密码**: admin123
- **角色**: 管理员

#### 学生账号
- **用户名**: 20160216002  
- **密码**: 123456
- **角色**: 学生

### 登录步骤
1. 访问 http://localhost:8080
2. 输入用户名和密码
3. 选择对应角色
4. 输入验证码
5. 点击登录

## 🛠️ 常见问题解决

### 问题1：数据库连接失败
**错误信息**: `Communications link failure`
**解决方案**:
```bash
# 检查MySQL是否启动
mysql -u root -p

# 检查端口是否正确
netstat -an | grep 3306

# 修改application.properties中的数据库密码
spring.datasource.password=你的实际密码
```

### 问题2：Redis连接失败
**错误信息**: `Unable to connect to Redis`
**解决方案**:
```bash
# 启动Redis服务
redis-server

# 检查Redis是否运行
redis-cli ping

# 检查端口
netstat -an | grep 6379
```

### 问题3：Maven依赖下载失败
**解决方案**:
```bash
# 清理Maven缓存
mvn clean

# 重新下载依赖
mvn dependency:resolve

# 或者删除.m2/repository目录重新下载
```

### 问题4：前端依赖安装失败
**解决方案**:
```bash
# 清理npm缓存
npm cache clean --force

# 删除node_modules重新安装
rm -rf node_modules
npm install

# 使用淘宝镜像
npm install --registry=https://registry.npm.taobao.org
```

### 问题5：端口被占用
**解决方案**:
```bash
# 查看端口占用情况
# Windows
netstat -ano | findstr :8999
netstat -ano | findstr :8080

# Linux/Mac
lsof -i :8999
lsof -i :8080

# 杀死占用端口的进程或修改配置文件中的端口
```

## 📱 访问系统

### 系统地址
- **前端地址**: http://localhost:8080
- **后端API**: http://localhost:8999/competition

### 功能验证
1. **登录功能**: 使用默认账号登录
2. **竞赛浏览**: 查看竞赛通知列表
3. **个人信息**: 查看和修改个人信息
4. **组队功能**: 创建或加入队伍
5. **管理功能**: 管理员发布竞赛和公告

## 🔧 开发模式

### 热重载开发
- **后端**: IDE中修改代码自动重启
- **前端**: `npm run serve` 支持热重载

### 调试模式
- **后端**: IDE中设置断点调试
- **前端**: 浏览器F12开发者工具

## 📦 生产部署

### 前端打包
```bash
cd 118/competitionapply-web
npm run build
# 生成dist目录，部署到nginx等web服务器
```

### 后端打包
```bash
cd 118/competition_apply_system
mvn clean package
# 生成jar文件，使用java -jar运行
```

---

**🎉 恭喜！按照以上步骤，您应该能够成功运行大学竞赛活动报名系统了！**

如遇到其他问题，请检查控制台错误信息或联系技术支持。
