# 数据库连接检查指南

## 🔍 检查数据库连接的方法

### 方法1：检查MySQL服务状态

**在PowerShell中运行**：
```powershell
# 检查MySQL服务是否运行
Get-Service | Where-Object {$_.Name -like "*mysql*"}

# 或者使用net命令
net start | findstr -i mysql

# 查看所有MySQL相关服务
sc query | findstr -i mysql
```

**成功的标志**：
```
Status        : Running
DisplayName   : MySQL80
ServiceName   : MySQL80
```

### 方法2：尝试连接数据库

**使用命令行连接**：
```powershell
# 尝试连接MySQL（会提示输入密码）
mysql -u root -p

# 连接成功后会看到：
# mysql>
```

**在MySQL命令行中检查**：
```sql
-- 显示所有数据库
SHOW DATABASES;

-- 检查项目数据库是否存在
USE db_competitionapply;

-- 显示数据表
SHOW TABLES;

-- 退出MySQL
EXIT;
```

### 方法3：检查端口连接

```powershell
# 检查3306端口是否有MySQL在监听
netstat -an | findstr :3306

# 应该看到类似：
# TCP    0.0.0.0:3306           0.0.0.0:0              LISTENING
```

### 方法4：使用telnet测试连接

```powershell
# 测试MySQL端口是否可连接
telnet localhost 3306

# 如果连接成功，会显示一些乱码（这是正常的）
# 按Ctrl+C退出
```

## 🔧 检查项目配置

### 查看数据库配置

根据您的 `application.properties` 文件：
```properties
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/db_competitionapply?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
spring.datasource.username=root
spring.datasource.password=root
```

**需要确认的配置项**：
- **数据库地址**: 127.0.0.1:3306 ✓
- **数据库名称**: db_competitionapply
- **用户名**: root
- **密码**: root（需要确认是否正确）

### 修改数据库密码（如果需要）

如果您的MySQL密码不是 `root`，需要修改：
```properties
spring.datasource.password=您的实际密码
```

## 🚀 完整检查流程

### 步骤1：检查MySQL服务

```powershell
# 检查服务状态
Get-Service | Where-Object {$_.Name -like "*mysql*"}
```

**如果服务未运行**：
```powershell
# 启动MySQL服务
net start mysql
# 或者
net start mysql80
```

### 步骤2：测试数据库连接

```powershell
# 尝试连接（输入您的MySQL密码）
mysql -u root -p
```

**连接成功后**：
```sql
-- 检查项目数据库
SHOW DATABASES;
-- 应该能看到 db_competitionapply

-- 使用项目数据库
USE db_competitionapply;

-- 检查数据表
SHOW TABLES;
-- 应该能看到 t_user, t_competition 等表

-- 退出
EXIT;
```

### 步骤3：验证配置文件

确认 `application.properties` 中的密码正确：
```properties
spring.datasource.password=您的实际MySQL密码
```

## 🚨 常见问题及解决方案

### 问题1：MySQL服务未启动

**错误现象**：
```
Get-Service : 找不到任何服务
```

**解决方案**：
```powershell
# 启动MySQL服务
net start mysql

# 如果提示服务名不存在，尝试：
net start mysql80
net start mysql57
```

### 问题2：密码错误

**错误现象**：
```
ERROR 1045 (28000): Access denied for user 'root'@'localhost'
```

**解决方案**：
1. 确认MySQL密码
2. 修改 `application.properties` 中的密码
3. 或者重置MySQL密码

### 问题3：数据库不存在

**错误现象**：
```
ERROR 1049 (42000): Unknown database 'db_competitionapply'
```

**解决方案**：
```sql
-- 创建数据库
CREATE DATABASE db_competitionapply CHARACTER SET utf8mb4;

-- 导入数据
USE db_competitionapply;
SOURCE D:/00Lesson/大四/综合实训/（118）大学竞赛活动报名系统/db_competitionapply.sql;
```

### 问题4：端口被占用

**检查端口占用**：
```powershell
netstat -ano | findstr :3306
```

## 🎯 快速检查命令

**复制以下命令到PowerShell中逐行执行**：

```powershell
# 1. 检查MySQL服务
Get-Service | Where-Object {$_.Name -like "*mysql*"}

# 2. 检查端口监听
netstat -an | findstr :3306

# 3. 尝试连接数据库
mysql -u root -p
# 输入密码后，如果连接成功：
# SHOW DATABASES;
# USE db_competitionapply;
# SHOW TABLES;
# EXIT;
```

## ✅ 连接成功的标志

### MySQL服务运行正常
```
Status        : Running
ServiceName   : MySQL80
```

### 端口监听正常
```
TCP    0.0.0.0:3306           0.0.0.0:0              LISTENING
```

### 数据库连接成功
```
mysql> SHOW DATABASES;
+--------------------+
| Database           |
+--------------------+
| db_competitionapply|
| information_schema |
| mysql              |
| performance_schema |
| sys                |
+--------------------+
```

### 项目数据表存在
```
mysql> USE db_competitionapply;
mysql> SHOW TABLES;
+---------------------------+
| Tables_in_db_competitionapply |
+---------------------------+
| t_advice                  |
| t_apply                   |
| t_competition             |
| t_user                    |
| ...                       |
+---------------------------+
```

---

**请先执行步骤1检查MySQL服务状态，告诉我结果如何！**
