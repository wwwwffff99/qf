# 后端服务启动指南

## 🎯 当前状态

✅ **已完成**：
- Redis服务（端口6379）
- 前端Vue.js服务（端口8080）
- Node.js兼容性问题修复

🔄 **待完成**：
- MySQL数据库确认
- 后端Spring Boot服务启动
- 系统功能测试

## 📋 第一步：检查MySQL数据库

### 方法1：检查MySQL服务状态

**在PowerShell中运行**：
```powershell
# 检查MySQL服务是否运行
Get-Service | Where-Object {$_.Name -like "*mysql*"}

# 或者使用cmd命令
net start | findstr MySQL
```

### 方法2：尝试连接数据库

```powershell
# 尝试连接MySQL（需要输入密码）
mysql -u root -p

# 连接成功后，检查数据库
SHOW DATABASES;
USE db_competitionapply;
SHOW TABLES;
```

### 方法3：如果MySQL未启动

```powershell
# 启动MySQL服务
net start mysql

# 或者
net start mysql80  # 根据您的MySQL版本
```

## 🚀 第二步：启动后端Spring Boot服务

### 方法1：使用Maven Wrapper（推荐）

```powershell
# 进入后端项目目录
cd "D:\00Lesson\大四\综合实训\（118）大学竞赛活动报名系统\118\competition_apply_system"

# 检查Maven Wrapper是否存在
dir mvnw.cmd

# 启动后端服务
.\mvnw.cmd spring-boot:run
```

### 方法2：使用IntelliJ IDEA（最简单）

1. **打开IntelliJ IDEA**
2. **File → Open** → 选择 `118\competition_apply_system` 目录
3. **等待项目加载完成**（IDEA会自动下载依赖）
4. **找到主类**：`CompetitionApplySystemApplication.java`
5. **右键 → Run 'CompetitionApplySystemApplication'**

### 方法3：如果Maven有问题

```powershell
# 检查Java版本
java -version

# 如果有jar文件，直接运行
cd "D:\00Lesson\大四\综合实训\（118）大学竞赛活动报名系统\118\competition_apply_system\target"
java -jar BookManagerApi-1.0.5.jar
```

## 🔧 第三步：解决可能的问题

### 问题1：MySQL连接失败

**检查配置文件**：`application.properties`
```properties
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/db_competitionapply
spring.datasource.username=root
spring.datasource.password=root  # 改为您的实际密码
```

### 问题2：端口被占用

```powershell
# 检查8999端口是否被占用
netstat -ano | findstr :8999

# 如果被占用，可以修改配置文件中的端口
# server.port=8999 改为 server.port=9000
```

### 问题3：Maven依赖下载失败

```powershell
# 清理Maven缓存
.\mvnw.cmd clean

# 重新下载依赖
.\mvnw.cmd dependency:resolve

# 重新启动
.\mvnw.cmd spring-boot:run
```

## 🎯 完整启动命令序列

**请按顺序执行以下命令**：

```powershell
# 1. 检查MySQL状态
Get-Service | Where-Object {$_.Name -like "*mysql*"}

# 2. 如果MySQL未运行，启动它
net start mysql

# 3. 进入后端项目目录
cd "D:\00Lesson\大四\综合实训\（118）大学竞赛活动报名系统\118\competition_apply_system"

# 4. 检查配置文件（确认数据库密码正确）
notepad src\main\resources\application.properties

# 5. 启动后端服务
.\mvnw.cmd spring-boot:run
```

## ✅ 成功启动的标志

### MySQL启动成功
```
服务名称: MySQL80
状态: Running
```

### 后端启动成功
```
  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/
 :: Spring Boot ::        (v2.5.6)

Started CompetitionApplySystemApplication in 15.234 seconds (JVM running for 18.567)
```

## 🧪 第四步：测试完整系统

### 1. 确认所有服务运行

```
✅ Redis     - localhost:6379
✅ MySQL     - localhost:3306  
✅ 后端API   - localhost:8999
✅ 前端界面  - localhost:8080
```

### 2. 测试登录功能

**回到前端页面** (http://localhost:8080)

**使用测试账号**：
- 用户名：`admin`
- 密码：`admin123`
- 角色：管理员

### 3. 验证系统功能

登录成功后，测试以下功能：
- 查看竞赛通知
- 个人信息管理
- 系统公告
- 统计分析（管理员）

## 🚨 常见启动问题

### 1. 数据库连接失败
```
Error: Could not connect to database
```
**解决**：检查MySQL服务状态和密码配置

### 2. 端口占用
```
Error: Port 8999 is already in use
```
**解决**：修改application.properties中的端口号

### 3. Maven依赖问题
```
Error: Could not resolve dependencies
```
**解决**：使用IDE自动下载依赖，或清理Maven缓存

## 📊 系统架构最终状态

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   前端 Vue.js   │    │  后端 Spring    │    │  数据库 MySQL   │
│   localhost:8080│◄──►│   localhost:8999│◄──►│   localhost:3306│
│       ✅        │    │       🔄        │    │       🔄        │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                ▲
                                │
                       ┌─────────────────┐
                       │   缓存 Redis    │
                       │   localhost:6379│
                       │       ✅        │
                       └─────────────────┘
```

---

**🚀 让我们开始！请先执行MySQL检查命令，告诉我结果如何！**
