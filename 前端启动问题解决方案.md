# 前端启动问题解决方案

## 🔍 问题分析

您遇到的错误：
```
'vue-cli-service' 不是内部或外部命令，也不是可运行的程序
```

**原因**: 前端项目的依赖包（node_modules）还没有安装。

## 🚀 解决方案

### 步骤1：检查Node.js和npm是否已安装

**在PowerShell中运行以下命令**：
```powershell
node --version
npm --version
```

**如果显示版本号**，说明已安装，继续下一步。
**如果提示"不是内部或外部命令"**，需要先安装Node.js。

### 步骤2：安装Node.js（如果需要）

1. **访问Node.js官网**：https://nodejs.org/
2. **下载LTS版本**（推荐18.x或20.x）
3. **运行安装程序**，按默认设置安装
4. **重启命令提示符**，重新检查版本

### 步骤3：安装前端项目依赖

**在前端项目目录中运行**：
```powershell
cd "D:\00Lesson\大四\综合实训\（118）大学竞赛活动报名系统\118\competitionapply-web"
npm install
```

**如果npm安装慢，使用淘宝镜像**：
```powershell
npm install --registry=https://registry.npmmirror.com
```

### 步骤4：启动前端服务

**依赖安装完成后**：
```powershell
npm run serve
```

## 🎯 完整的启动命令序列

**复制以下命令到PowerShell中逐行执行**：

```powershell
# 1. 进入前端项目目录
cd "D:\00Lesson\大四\综合实训\（118）大学竞赛活动报名系统\118\competitionapply-web"

# 2. 检查Node.js版本
node --version

# 3. 检查npm版本  
npm --version

# 4. 安装依赖（使用淘宝镜像，速度更快）
npm install --registry=https://registry.npmmirror.com

# 5. 启动前端服务
npm run serve
```

## 🔧 如果仍有问题

### 问题1：Node.js未安装
**解决方案**：
1. 下载Node.js：https://nodejs.org/zh-cn/
2. 选择LTS版本（长期支持版）
3. 运行安装程序，使用默认设置
4. 重启PowerShell，重新尝试

### 问题2：npm安装依赖失败
**解决方案**：
```powershell
# 清理npm缓存
npm cache clean --force

# 删除已有的node_modules（如果存在）
Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue

# 重新安装
npm install --registry=https://registry.npmmirror.com
```

### 问题3：权限问题
**解决方案**：
1. **以管理员身份运行PowerShell**
2. 或者配置npm全局目录：
```powershell
npm config set prefix "C:\Users\%USERNAME%\AppData\Roaming\npm"
```

### 问题4：网络问题
**解决方案**：
```powershell
# 使用不同的镜像源
npm install --registry=https://registry.npmmirror.com

# 或者使用cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install
```

## 📋 系统启动完整流程

### 当前状态检查：
- [x] ✅ **Redis已启动**（端口6379）
- [ ] 🔄 **MySQL数据库** - 需要确认
- [ ] 🔄 **后端服务** - Maven问题待解决
- [ ] 🔄 **前端服务** - 当前步骤，依赖安装中

### 推荐启动顺序：
1. **Redis** ✅ 已完成
2. **MySQL** - 确保数据库服务运行
3. **前端依赖安装** ← 当前步骤
4. **后端服务启动**
5. **前端服务启动**

## 🎯 成功启动的标志

**前端启动成功后，您会看到**：
```
 App running at:
 - Local:   http://localhost:8080/
 - Network: http://192.168.x.x:8080/

 Note that the development build is not optimized.
 To create a production build, run npm run build.
```

## 💡 小贴士

### 加速npm安装：
```powershell
# 设置淘宝镜像为默认源
npm config set registry https://registry.npmmirror.com

# 验证设置
npm config get registry
```

### 查看安装进度：
```powershell
# 显示详细安装信息
npm install --verbose
```

### 如果安装卡住：
```powershell
# 强制停止：Ctrl + C
# 清理缓存后重试
npm cache clean --force
npm install
```

---

**按照以上步骤操作，前端应该能够成功启动！如果遇到具体错误，请告诉我错误信息。**
