# 密码错误问题最终解决方案

## 🎉 问题确认

从Network响应可以看到：
```json
{"status":"-2","msg":"密码错误","data":{}}
```

**问题分析**：
- ✅ 前后端通信正常
- ✅ 后端服务正常运行
- ❌ 密码验证失败
- ❌ 前端没有正确处理错误响应

## 🔐 密码问题解决方案

### 方案1：尝试其他已知用户

根据数据库查询结果，尝试以下用户：

**用户1**：
- 用户名：`20160216010`
- 密码：`123456`
- 角色：学生

**用户2**：
- 用户名：`20160216011`
- 密码：`123456`
- 角色：学生

### 方案2：生成正确的admin密码

根据UserRealm.java中的加密方法，admin的密码需要特殊生成。

**可能的admin密码组合**：
1. `admin` / `admin`
2. `admin` / `123456`
3. `admin` / `password`

### 方案3：重置admin密码

**在MySQL中执行**：
```sql
-- 查看admin用户的当前密码（加密后的）
SELECT user_name, password FROM t_user WHERE user_name = 'admin';

-- 如果需要，可以临时设置一个已知的密码
-- 首先需要用UserRealm.java的func()方法生成正确的MD5密码
```

## 🎯 立即测试方案

### 测试1：使用学生账号

**立即尝试**：
- 用户名：`20160216010`
- 密码：`123456`
- 角色：学生
- 验证码：正确输入

### 测试2：尝试不同的admin密码

**按顺序尝试**：
1. admin / admin
2. admin / 123456
3. admin / password

## 🔧 如果所有密码都不对

### 方案：生成测试密码

**使用UserRealm.java中的方法**：
```java
@Test
public void func(){
    String fun = "MD5";
    String pwd = "123456";  // 设置简单密码
    ByteSource credentialsSalt = ByteSource.Util.bytes("admin");  // 用户名作为盐值
    int i = 1024;
    SimpleHash simpleHash = new SimpleHash(fun, pwd, credentialsSalt, i);
    System.out.println("admin用户密码123456的加密结果: " + simpleHash);
}
```

**然后更新数据库**：
```sql
UPDATE t_user SET password = '生成的加密密码' WHERE user_name = 'admin';
```

## 💡 最快解决方案

### 立即尝试学生账号

**这是最快的方法**：
1. **用户名**：`20160216010`
2. **密码**：`123456`
3. **角色**：学生
4. **验证码**：正确输入

**如果学生账号能登录成功，说明系统完全正常，只是admin密码的问题。**

## 🎊 系统状态

```
✅ Redis服务      - localhost:6379 (运行中)
✅ MySQL数据库    - localhost:3306 (运行中)
✅ 前端Vue.js服务 - localhost:8080 (运行中)
✅ 后端API服务    - localhost:8999 (运行中)
✅ 前后端通信     - 正常
🔄 登录验证      - 密码问题待解决
```

## 🚀 下一步行动

1. **立即测试学生账号登录**
2. **如果成功，系统就完全可用了！**
3. **然后可以在系统内部修改admin密码**

---

**请立即尝试学生账号：20160216010 / 123456 / 学生角色！**
