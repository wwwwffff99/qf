# 密码问题解决方案

## 🔍 问题分析

从您的 `UserRealm.java` 文件可以看到，系统使用了**MD5加密 + 盐值 + 1024次迭代**的方式来加密密码。

### 密码加密方式
```java
// 加密算法：MD5
// 盐值：用户名
// 迭代次数：1024次
String fun = "MD5";
String pwd = "123456";
ByteSource credentialsSalt = ByteSource.Util.bytes("20160216106"); // 用户名作为盐值
int i = 1024;
SimpleHash simpleHash = new SimpleHash(fun, pwd, credentialsSalt, i);
```

## 🔧 解决方案

### 方案1：使用正确的测试账号和密码

根据README.md中的说明，密码加密方式需要通过 `UserRealm.java` 的 `func()` 方法生成。

**让我们查看数据库中的实际用户数据**：

```sql
-- 连接数据库
mysql -u root -p

-- 使用项目数据库
USE db_competitionapply;

-- 查看用户表中的实际数据
SELECT user_id, user_name, password, name FROM t_user LIMIT 10;
```

### 方案2：生成正确的加密密码

**创建密码生成工具类**：

```java
// 在UserRealm.java中已有的func()方法
@Test
public void func(){
    String fun = "MD5";
    String pwd = "admin123";  // 原始密码
    ByteSource credentialsSalt = ByteSource.Util.bytes("admin");  // 用户名作为盐值
    int i = 1024;
    SimpleHash simpleHash = new SimpleHash(fun, pwd, credentialsSalt, i);
    System.out.println("加密后的密码: " + simpleHash);
}
```

### 方案3：使用数据库中现有的用户

**查看数据库中的用户信息**：

```sql
-- 查看所有用户
SELECT user_id, user_name, name, password FROM t_user;

-- 查看管理员用户
SELECT user_id, user_name, name, password FROM t_user WHERE user_name = 'admin';

-- 查看学生用户
SELECT user_id, user_name, name, password FROM t_user WHERE user_name LIKE '2016%';
```

## 🎯 立即解决步骤

### 步骤1：查看数据库中的实际用户

**请在MySQL中执行**：
```sql
mysql -u root -p
USE db_competitionapply;
SELECT user_id, user_name, name, password FROM t_user LIMIT 5;
```

### 步骤2：尝试使用数据库中的实际用户

根据之前导入的数据，可能的用户账号：

**用户1**：
- 用户名：`admin`
- 原始密码：可能是 `admin123` 或其他
- 角色：管理员

**用户2**：
- 用户名：`20160216002`
- 原始密码：可能是 `123456`
- 角色：学生

### 步骤3：如果需要重置密码

**创建新的测试用户**：
```sql
-- 为了测试，我们可以创建一个简单密码的用户
-- 首先生成MD5加密密码（用户名：test，密码：123456）

-- 或者直接使用已知的加密密码
INSERT INTO t_user (user_id, user_name, password, name, phone, email, sex, period, college_id, user_role, user_className) 
VALUES ('999', 'test', '已知的MD5密码', '测试用户', '12345678901', 'test@test.com', '男', 2024, '1', null, '测试班');
```

## 🧪 测试不同的登录组合

### 尝试以下账号组合：

1. **用户名**: `admin`，**密码**: `admin123`
2. **用户名**: `20160216002`，**密码**: `123456`
3. **用户名**: `admin`，**密码**: `123456`
4. **用户名**: 数据库中的其他用户名

## 🔍 调试方法

### 查看后端日志

在后端控制台中应该能看到登录尝试的日志，包括：
- 用户名是否找到
- 密码比对结果

### 检查前端网络请求

在浏览器开发者工具的Network标签中，查看登录请求的响应，了解具体的错误信息。

## 💡 快速解决方案

**最简单的方法**：

1. **查看数据库实际用户**：
```sql
SELECT user_name, name FROM t_user WHERE user_name IN ('admin', '20160216002', '20160216003');
```

2. **尝试常见密码组合**：
   - admin / admin
   - admin / 123456
   - admin / admin123
   - 20160216002 / 123456

3. **如果都不行，查看数据库中用户的完整信息**，然后根据README.md的说明生成正确的密码。

---

**请先执行SQL查询，查看数据库中的实际用户信息，然后告诉我结果！**
